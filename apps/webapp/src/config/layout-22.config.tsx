import { APP_HEADER_MENU, APP_NAVBAR_MENU } from './menu.config';
import { type MenuConfig } from './types';

export const MENU_HEADER: MenuConfig = APP_HEADER_MENU;
export const MENU_NAVBAR: MenuConfig = APP_NAVBAR_MENU;
