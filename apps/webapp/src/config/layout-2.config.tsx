import { APP_HEADER_MENU, APP_ICON_RAIL_MENU } from './menu.config';
import { type MenuConfig } from './types';

export const MENU_HEADER: MenuConfig = APP_HEADER_MENU;
export const MENU_ROOT: MenuConfig = APP_ICON_RAIL_MENU;
