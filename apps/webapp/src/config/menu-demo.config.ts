/**
 * menu-demo.config.ts
 *
 * Sauvegarde de référence des menus Metronic originaux (layout-1 & layout-6).
 * Ce fichier n'est PAS importé par l'application.
 * Il sert uniquement de référence pour retrouver le contenu démo d'origine.
 *
 * @see layout-1.config.tsx — MENU_SIDEBAR, MENU_MEGA, MENU_MEGA_MOBILE
 * @see layout-6.config.tsx — MENU_SIDEBAR_COMPACT, MENU_ROOT
 *
 * Le contenu original complet est conservé dans l'historique git
 * (commit précédant l'introduction de menu.config.ts).
 */

export {};
