import { Users } from '@/components/keenicons/icons';
import type { MenuItem } from '@/config/types';

export const meetingMenuItems: MenuItem[] = [
  {
    title: 'Réunions',
    icon: Users,
    path: '/meetings',
  },
];
