import { UserCircle } from '@/components/keenicons/icons';
import type { MenuItem } from '@/config/types';

export const profileMenuItems: MenuItem[] = [
  {
    title: 'Mon Profil',
    icon: UserCircle,
    path: '/onboarding',
  },
];
